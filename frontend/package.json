{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy --branch production",
    "deploy:preview": "npm run build && npx wrangler pages deploy",
    "db:init": "npx wrangler d1 execute food-ordering-db --file=./migrations/0001_schema.sql",
    "db:seed": "npx wrangler d1 execute food-ordering-db --command=\"INSERT INTO ingredients ...\""
  },
  "dependencies": {
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.12",
    "wrangler": "^4.99.0"
  }
}
